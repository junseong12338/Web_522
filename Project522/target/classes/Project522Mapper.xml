<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project522.mapper.CommunityMapper">
	<select id="viewAll" resultType="com.project522.domain.CommunityDTO">
		SELECT * FROM community
	</select>
	
	<select id="getList" resultType="com.project522.domain.CommunityDTO">
		SELECT community_num,community_category,community_title,community_date,community_map,user_id,community_place,community_schedule FROM community
	</select>

	<insert id="insertCommunity"
		parameterType="com.project522.domain.CommunityDTO">
		Insert into community(community_num,community_category,community_title, community_context, community_date) 
		values(#{community_num}, #{community_category}, #{community_title}, #{community_context}, TO_DATE(sysdate,'yyyy.mm.dd hh24:mi'))
	</insert>
</mapper>